// Класс в котором хранится информация по датчику получаемая из configure
class ConfSensor {
  final int id;
  final String name;
  final int defaultValue;

  ConfSensor({this.id, this.name, this.defaultValue});
// возможно добавятся поля
}
